import java.util.Scanner;

//TEMAT 2

/*
 	2. Wczytaæ od u¿ytkownika 3 liczby ca³kowite i wypisaæ na ekran najwiêksz¹ oraz
	najmniejsz¹ z nich.
*/

/*
public class zadanie2
{
	public static void main(String[] args)
	{
		int a=0, b=0, c=0;
		int max=0, min=0;
		Scanner in = new Scanner(System.in);
		System.out.println("Wpisz 3 liczby a powiem Ci, która z nich jest najwieksza! ");
		a = in.nextInt();
		b = in.nextInt();
		c = in.nextInt();
		
		max = (a>b ? a : b);
		max = (c>max ? c : max);
		
		min = (a<b ? a : b);
		min = (c<min ? c : min);
		
		System.out.println("Najwieksza liczba to: " + max + ", a najmniejsza to: " + min);
	}
}
*/

// *TIP:    if (a > b) return a ;
//			else return b;
//
//			zamiast powyzszego kodu mozna uzyc (a>b ? a : b);